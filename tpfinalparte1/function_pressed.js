function botonEstado(x, y, r) {
  let distancia = dist(mouseX, mouseY, x, y);
  
  if(distancia < r) {
    fill(172,144,108);
  }else{
    fill(187,144,102);
}

ellipse(x, y, r * 2, r * 2);
}

function botonEstado2(x, y, r) {
  let distancia = dist(mouseX, mouseY, x, y);
  
  if(distancia < r) {
    fill(172,144,108);
  }else{
    fill(187,144,102);
}

  ellipse(x, y, r * 2, r * 2);
}

  function botonEstado3(x, y, r) {
  let distancia = dist(mouseX, mouseY, x, y);
  
  if(distancia < r) {
    fill(172,144,108);
  }else{
    fill(187,144,102);
}

ellipse(x, y, r * 2, r * 2);

}

function botonEstado4(x, y, r) {
  let distancia = dist(mouseX, mouseY, x, y);
  
  if(distancia < r) {
    fill(172,144,108);
  }else{
    fill(187,144,102);
}

ellipse(x, y, r * 2, r * 2);

}

function botonEstado5(x, y, r) {
  let distancia = dist(mouseX, mouseY, x, y);
  
  if(distancia < r) {
    fill(172,144,108);
  }else{
    fill(187,144,102);
}

ellipse(x, y, r * 2, r * 2);

}
